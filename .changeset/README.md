# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

To record a change for a release, run:

```sh
bun changeset
```

The version PR will be opened automatically by the release workflow.
